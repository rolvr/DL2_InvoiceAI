"""Shared utility package for the invoice-image-processing project.

Every member notebook imports from here instead of duplicating logic. Keep functions small,
documented, and framework-agnostic where possible so both the TensorFlow and PyTorch users on
the team can use them.
"""
