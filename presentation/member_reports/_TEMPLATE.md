# Report Log — <Member Name> (<Role>)

_Last updated: <YYYY-MM-DD HH:MM TZ>_

## 1. Objective (what I was responsible for)
<1–3 sentences: your slice of the pipeline and what "done" means for you.>

## 2. What I did
<Chronological or grouped bullet list of the concrete work: datasets loaded, models
trained/used, notebook sections implemented, files produced. Be specific.>

## 3. Approach & key decisions
<The methods/architecture/libraries you chose and WHY. Note any decision where you picked one
option over another (e.g. EasyOCR vs Tesseract, SSD vs YOLO fallback, sample size), and the
reasoning. This is prime slide/report material.>

## 4. Challenges faced & how I handled them
<The most valuable section for the report. For each challenge: what went wrong or was hard,
what you tried, and the outcome/workaround. Include things that are still unresolved.>
- **Challenge:** …
  - **Resolution / status:** …

## 5. Results & metrics
<Concrete numbers: dataset counts, split sizes, precision/recall/IoU, OCR coverage, etc. Point
to the exact files (`outputs/metrics/*.json`, `outputs/figures/*.png`) that back them up.>

## 6. Assumptions & limitations
<What you assumed, what the results do NOT show, and where accuracy/scope is limited. Honesty
here strengthens the report.>

## 7. Handoff notes (for downstream members / integration)
<What the next member(s) need to know to consume your outputs: file paths, schema quirks,
join keys, gotchas.>

## 8. Figures / artifacts to consider for the slide deck
<List specific figures or outputs worth putting on a slide, with a one-line caption each.>
